{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|Grep|Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scratchpad-nudge.sh"
          }
        ]
      }
    ]
  }
}
